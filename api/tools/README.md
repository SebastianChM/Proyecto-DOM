# Debug & Maintenance Tools

This directory contains scripts for debugging, testing, and maintenance.

**WARNING**: These scripts are NOT part of the production runtime and should not be included in builds.

## Usage

Run these scripts using `ts-node` explicitly:

```bash
npx ts-node api/tools/<script-name>.ts
```
